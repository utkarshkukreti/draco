[package]
name = "jfb"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
draco = { path = "../.." }
xorshift = "0.1"
wasm-bindgen = { version = "0.2", features = ["enable-interning"] }
web-sys = { version = "0.3", features = ["Window"] }
